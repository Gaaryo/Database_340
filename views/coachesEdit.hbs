<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Coaches Edit Page</title>
        <link rel="stylesheet" href="../css/style.css">
    </head>
    <body>
        
        
        <div class="contents">
            <h2>
                Coaches Edit
            </h2>
            <div class="create">
                <form id="add-coach-form" method="POST" action="/add-coach-form">
                    <h3>Create New Data</h3>
                    <div class="u_input">
                        <label for="input-first_name">First Name</label>
                        <input  name="input-first_name" id="input-first_name">
                        <p>*require</p>
                    </div>
                    <div class="u_input">
                        <label for="input-last_name">Last Name </label>
                        <input type="text" name="input-last_name" id="input-last_name">
                        <p>*require</p>
                    </div>
                    
                    <input type="submit">
                </form>
            </div>       
            
            <div>
                <table id="coaches-table">
                    <thead>

                        <tr>
                            
                            <th>ID</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>delete</th>
                        </tr>
                    </thead>

                    {{!-- Body section --}}
                    <tbody>

                        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
                        {{#each data}}
                        <tr data-value={{this.coach_id}}>
                            <td>{{this.coach_id}}</td>
                            <td>{{this.first_name}}</td>
                            <td>{{this.last_name}}</td>
                            <td><button class= "delete_button" onclick="deleteCoach({{this.coach_id}})"> - </button></td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                <script src="./js/delete_coach.js"></script>
            </div>
            
        </div>
       


    </body>
</html>